/**
 * Expresiones
 */
export const alphaNumeric: RegExp = RegExp(/([A-Za-z0-9])/)


/**
 * Expressions shortcuts
 */
export const alphaNumericShort: RegExp = RegExp(/(\w)/)
export const numericShort: RegExp = RegExp(/(\d)/)
